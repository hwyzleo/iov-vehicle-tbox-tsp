# Invalid: topic_template uses deprecated {device_sn} variable (schema fail)
tsp:
  ipc:
    socket_path: /tmp/tbox-tsp.sock
    downlink_queue_size: 256
    slow_subscriber_policy: disconnect
  mqtt:
    socket_path: /tmp/tbox-mqtt.sock
  subscriptions:
    - route_id: fota.uplink
      topic_template: vehicle/{device_sn}/up/fota
      direction: UP
      qos: 1
      target: tsp.fota
      mandatory: false
